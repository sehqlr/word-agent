# I'm going to make a vagrant virtual machine for Word Agent development.
# I hope that will solve some of my development problems, and it will allow
# for more room to experiment with Docker et al without 'damaging' my local machine

Vagrant.configure("2") do |config|
  config.vm.box = "coreos-alpha"
end
